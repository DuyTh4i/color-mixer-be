[project]
name = "color-mixer-be"
version = "0.1.0"
dependencies = [
    "fastapi[standard]",
    "supabase",
    "slowapi",
    "numpy",
    "scipy"
]

[tool.setuptools]
py-modules = ["main", "utils"]
